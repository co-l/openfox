{
  "metadata": {
    "id": "default",
    "name": "Build & Verify",
    "description": "Standard build-verify loop. Builder implements criteria, verifier checks them.",
    "version": "1.0.0"
  },
  "entryStep": "build",
  "settings": {
    "maxIterations": 50,
    "maxVerifyRetries": 4
  },
  "steps": [
    {
      "id": "build",
      "name": "Builder",
      "type": "llm_turn",
      "phase": "build",
      "toolMode": "builder",
      "kickoffPrompt": "Begin implementing the acceptance criteria. Mark each criterion as complete using complete_criterion when done.",
      "nudgePrompt": "Continue working on the acceptance criteria. {{reason}}.\n\n{{verifierFindings}}\nDon't forget to mark the criteria as complete with complete_criterion",
      "transitions": [
        { "when": { "type": "any_criteria_blocked" }, "goto": "$blocked" },
        { "when": { "type": "all_criteria_completed_or_passed" }, "goto": "verify" },
        { "when": { "type": "always" }, "goto": "build" }
      ]
    },
    {
      "id": "verify",
      "name": "Verifier",
      "type": "sub_agent",
      "phase": "verification",
      "subAgentType": "verifier",
      "transitions": [
        { "when": { "type": "any_criteria_blocked" }, "goto": "$blocked" },
        { "when": { "type": "all_criteria_passed" }, "goto": "$done" },
        { "when": { "type": "always" }, "goto": "build" }
      ]
    }
  ]
}
