/**
 * Sub-Agent Types
 *
 * Legacy type kept for backward compatibility during migration.
 * New code should use AgentDefinition from ../agents/types.ts.
 */

/**
 * @deprecated Use agent IDs from the agent registry instead.
 */
export type SubAgentType = string
